<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.deli2matching.dao.UserDao">

    <insert id="insert" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users (login_id, password, email, nickname, provider, provider_id)
        VALUES (#{loginId}, #{password}, #{email}, #{nickname}, #{provider}, #{providerId})
    </insert>

    <select id="findByLoginId" resultType="userEntity">
        SELECT user_id, login_id, password, email, nickname, provider, provider_id
        FROM users
        WHERE login_id = #{loginId}
        LIMIT 1
    </select>

    <select id="existsByLoginId" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM users
        WHERE login_id = #{loginId}
    </select>

    <select id="findById" resultType="userEntity">
        SELECT user_id, login_id, password, email, nickname, provider, provider_id
        FROM users
        WHERE user_id = #{id}
        LIMIT 1
    </select>

</mapper>
